# forks
Forks of upstream projects

# forking
To fork a project, run the following commands (be sure to change the branch and repo below):
```
git clone --branch v2.10.2 https://github.com/terraform-aws-modules/terraform-aws-route53
rm -rf terraform-aws-route53/.git
```
